## Notes on schedulling for devices

See **`devcontrol.yml`** for scheduling syntax.

- Shelly Smart Plugs also allow to schedule them under its own control web page
- Zigbee devices schedule relies on this App, they have not internal scheduling
